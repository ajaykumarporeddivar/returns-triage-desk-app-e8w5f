import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import Link from 'next/link';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Returns Triage Desk — Streamline E-commerce Returns',
  description: 'Returns Triage Desk helps e-commerce operations managers at Shopify stores centralize, prioritize, and report on return requests to reduce margin loss and manual effort.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-zinc-50 antialiased`}>
        <div className="fixed top-0 left-0 right-0 z-50 bg-zinc-900 text-zinc-100 text-xs px-4 py-2 flex justify-between items-center">
          <span>⚡ Demo Mode — Returns Triage Desk · Built with NEXUS OS</span>
          <Link href="/dashboard/triage-dashboard" className="text-zinc-300 hover:text-white transition-colors duration-200">
            Open Dashboard →
          </Link>
        </div>
        <div className="pt-9">
          {children}
        </div>
      </body>
    </html>
  );
}